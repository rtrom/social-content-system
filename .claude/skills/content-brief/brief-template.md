# Content Brief — Template

The anchor shape for `content-brief`. Kept deliberately short — this is the coherence anchor every channel binds to, not a document. Saved as `content-brief.md` (working directory) and filed as `YYYY-MM-DD_<release-slug>_brief.md` in the release directory; the saved file starts with the `# Content Brief` header (drop the "— Template") so every channel skill can resolve it on demand via the `content-brief` **resolve** routine.

---

## Content Brief
**Release slug:** <!-- becomes the shared naming slug for the whole release -->
**Date:** YYYY-MM-DD
**Niche:**

### Core message
<!-- Exactly one sentence. The single claim this release makes. -->

### Key points
<!-- 3–5, ordered by importance. Every channel draws from THIS set. -->
1.
2.
3.

### CTA
<!-- The one action asked of the reader. Phrasing adapts per platform; the ask does not. -->

### Tone notes
<!-- The delta from the standing brand voice for THIS piece. Not a restatement of the profile. -->

### Angle hook
<!-- Why this lands now. Usually inherited from the trend-research signal. -->

---

*Generated by the `content-brief` skill; saved as `content-brief.md` / `<date>_<slug>_brief.md`. Loaded by every channel writer via the `content-brief` resolve routine.*
